---
- name: Validate
  ansible.builtin.assert:
    that: oci_disaster_recovery_compartment_id is defined
    quiet: true

- name: Execute oci disaster recovery
  ansible.builtin.debug:
    msg: "Configure DR with cross-region replication"
