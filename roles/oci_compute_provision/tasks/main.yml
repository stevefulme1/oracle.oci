---
- name: Validate
  ansible.builtin.assert:
    that: oci_compute_provision_compartment_id is defined
    quiet: true

- name: Execute oci compute provision
  ansible.builtin.debug:
    msg: "Provision compute instances with block storage"
